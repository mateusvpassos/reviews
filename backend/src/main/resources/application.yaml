spring:
    profiles:
        active: ${APP_PROFILE:test}
    jpa:
        open-in-view: false
security:
    oauth2:
        client:
            client-id: ${CLIENT_ID:reviews}
            client-secret: ${CLIENT_SECRET:reviews123}
jwt:
    secret: ${JWT_SECRET:MY-JWT-SECRET}
    duration: ${JWT_DURATION:86400}